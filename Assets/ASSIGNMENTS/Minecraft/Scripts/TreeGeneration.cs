using UnityEngine;

public class TreeGeneration : MonoBehaviour
{
    public float MaxTreeHeight = 7;
    public float MaxTreeWidth = 5;

    void GenerateTree(Vector3Int origin, int height, int width)
    {
        for (int i = 0; i < height; i++)
        {
            //placeBlock(3, origin + new Vector3Int(origin.x, i, origin.z));
        }
        
        
    }
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }
    
    // Update is called once per frame
    void Update()
    {
        
    }
}
